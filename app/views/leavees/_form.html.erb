<%= form_for @leavee, url: employer_employee_leavees_path(params[:employer_id], params[:employee_id]) do |form| %>
  <% if leavee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(leavee.errors.count, "error") %> prohibited this leavee from being saved:</h2>

      <ul>
      <% leavee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :leave_from_date %>
    <%= form.text_field :leave_from_date, id: :leavee_leave_from_date %>
  </div>

  <div class="field">
    <%= form.label :leave_to_date %>
    <%= form.text_field :leave_to_date, id: :leavee_leave_to_date %>
  </div>

  <div class="field">
    <%= form.label :reason %>
    <%= form.text_area :reason, id: :leavee_reason , :rows => "4"%>
  </div>

  <div class="field">
    <%= form.label :no_of_days %>
    <%= form.text_field :no_of_days, id: :leavee_no_of_days %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
